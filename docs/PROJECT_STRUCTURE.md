# Project Structure

`Duck` is a Rojo-managed Roblox project. The source files in this repository map into Roblox services through `default.project.json`.

## Root Files

- `default.project.json`: Rojo project tree and service mappings.
- `aftman.toml`: Aftman-managed tool versions.
- `wally.toml`: Wally package dependencies.
- `wally.lock`: Locked Wally dependency versions after package install.
- `README.md`: Project overview and quickstart.
- `AGENTS.md`: Instructions for AI coding agents.
- `.gitignore`: Generated place files, Studio locks, sourcemaps, Wally packages, and temporary generated design outputs.

## Asset Folders

### `assets/design/references`

Use for style references, sketches, screenshots, and inspiration.

### `assets/design/generated`

Use for temporary Nano Banana Pro outputs and experiments. This folder is ignored by Git because generated images can become large quickly.

### `assets/design/approved`

Use for generated or edited images approved as useful project direction.

### `assets/ui/icons`

Use for final or likely-final UI icon assets.

### `assets/ui/backgrounds`

Use for UI backgrounds that may be used in Roblox.

### `assets/ui/mockups`

Use for approved UI layout mockups.

See `docs/ASSET_WORKFLOW.md` for asset rules.

## Source Folders

### `Packages`

Mapped to:

```text
ReplicatedStorage
  Packages
```

This folder is generated by Wally and should not be edited by hand. Run `wally install` to regenerate it from `wally.toml` and `wally.lock`.

Current approved packages:

- `React`
- `ReactRoblox`

### `src/client`

Mapped to:

```text
StarterPlayer
  StarterPlayerScripts
    Client
```

Use this folder for LocalScripts and client-only modules. Client code should handle presentation, input, camera behavior, UI, and local feedback. It must not be trusted for authoritative gameplay decisions.

Current files:

- `init.client.luau`
- `App.luau`
- `Screens/FarmScreen.luau`
- `Components/ActionButton.luau`
- `Components/ResourceCounter.luau`
- `UI/Theme.luau`

### `src/server`

Mapped to:

```text
ServerScriptService
  Server
```

Use this folder for server scripts and server-only modules. Server code should own authoritative state, validation, persistence, and any logic that affects other players.

Current files:

- `init.server.luau`
- `GameServer.luau`
- `PlayerStateService.luau`
- `PlayerDataService.luau`

### `src/shared`

Mapped to:

```text
ReplicatedStorage
  Shared
```

Use this folder for modules that are safe to require from both client and server. Shared modules should avoid runtime assumptions that only work on one side.

Current files:

- `AssetIds.luau`
- `PrototypeConfig.luau`
- `RemoteProtocol.luau`
- `Types.luau`

## Project Analysis Tools

### `docs/project-map`

Static HTML/CSS/JS dashboards for visually analyzing the repository and gameplay plan.

- `docs/project-map/index.html`: interactive project map for Rojo mappings, module dependencies, runtime remotes, server-owned state, save-data flow, UI screens, high-level gameplay loop, supporting gameplay systems, roadmap direction, and key docs.
- `docs/project-map/gameplay-flow.html`: autoplay box-component gameplay simulator for the current confirmed farm loop, including eggs, selling, care, shop items, quests, duck XP, weather, and save-state flow.

Open either file directly in a browser. These pages have no package install or build step and are not mapped into Roblox by Rojo.

## Runtime Remotes

The server creates these runtime instances under `ReplicatedStorage`:

```text
ReplicatedStorage
  DuckRemotes
    DuckAction
    DuckState
    DuckNotice
```

- `DuckAction`: client-to-server `RemoteEvent` for named UI actions. The `care_for_ducks` action may include a validated `duckIndex` payload for the selected duck; `buy_shop_item` may include a validated `itemId`; `rename_duck` may include a validated `duckIndex` and filtered display name request; `visit_farm` may include a validated same-server target `UserId`.
- `DuckState`: server-to-client `RemoteEvent` for serialized farm state, including Quest V0 summaries and Farm Visit V0 view metadata for client display.
- `DuckNotice`: server-to-client `RemoteEvent` for one-off notices such as Egg Catch session starts, Egg Catch reward results, and daily check-in claim or rejection feedback.

`DuckAction` includes gameplay actions and Studio-only tester actions, including the `test_toggle_weather` Studio action. Tester actions must stay guarded on the server with `RunService:IsStudio()` and must not become production rewards.

The names live in `src/shared/RemoteProtocol.luau`. Do not add new remotes or payload shapes without updating the protocol, security expectations, and docs.

## Roblox Services in the Rojo Tree

The current Rojo tree defines:

- `ReplicatedStorage.Packages`
- `ReplicatedStorage.Shared`
- `ServerScriptService.Server`
- `StarterPlayer.StarterPlayerScripts.Client`
- `Workspace.Baseplate`
- `Lighting`
- `SoundService`

Do not change these mappings unless the change is intentional and documented.

## Generated and Local Files

The following files are generated or local-only and should not be edited by hand:

- `Duck.rbxlx`
- `*.rbxlx.lock`
- `*.rbxl.lock`
- `sourcemap.json`
- `Packages`
